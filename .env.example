DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"
NEXTAUTH_URL="http://localhost:3000"

# VAPID keys for Web Push (generate with: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:brandi@nowakdental.com"

# Resend — sends attendee invitation emails.
# Sign up at https://resend.com → API Keys → Create API Key.
# Leave empty to disable email sending (admin still gets credentials as CSV).
RESEND_API_KEY=""
# Optional: verified sender. Use "onboarding@resend.dev" to test before verifying a domain.
RESEND_FROM="NOLA LabFest <onboarding@resend.dev>"
